<div className="movie-details">
      {movie ? (
        <>
          <h1>{movie.title}</h1>
          <p>{movie.overview}</p>
          <img src={`https://image.tmdb.org/t/p/w500${movie.poster_path}`} alt={movie.title} />
        </>
      ) : (
        <p>Movie not found</p>
      )}
    </div>